[project]
name = "orxhestra-code"
version = "0.0.29"
description = "AI coding agent powered by orxhestra — reads, writes, edits code and runs commands"
readme = "README.md"
requires-python = ">=3.10"
license = "Apache-2.0"
authors = [
    {name = "Nicolai M. T. Lassen"},
]
classifiers = [
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Operating System :: OS Independent",
]
dependencies = [
    "orxhestra[cli]>=0.0.58",
    "pyyaml>=6.0",
    "aiosqlite>=0.20",
    "sqlalchemy[asyncio]>=2.0",
    "ddgs>=9.0.0",
    "httpx>=0.28.0",
    "trafilatura>=1.12.0",
]

[project.optional-dependencies]
openai = ["orxhestra[openai]>=0.0.58"]
anthropic = ["orxhestra[anthropic]>=0.0.58"]
google = ["orxhestra[google]>=0.0.58"]
google-vertexai = ["orxhestra[google-vertexai]>=0.0.58"]
aws = ["orxhestra[aws]>=0.0.58"]
azure-ai = ["orxhestra[azure-ai]>=0.0.58"]
mistral = ["orxhestra[mistral]>=0.0.58"]
cohere = ["orxhestra[cohere]>=0.0.58"]
fireworks = ["orxhestra[fireworks]>=0.0.58"]
together = ["orxhestra[together]>=0.0.58"]
groq = ["orxhestra[groq]>=0.0.58"]
nvidia = ["orxhestra[nvidia]>=0.0.58"]
huggingface = ["orxhestra[huggingface]>=0.0.58"]
deepseek = ["orxhestra[deepseek]>=0.0.58"]
ollama = ["orxhestra[ollama]>=0.0.58"]
xai = ["orxhestra[xai]>=0.0.58"]
ibm = ["orxhestra[ibm]>=0.0.58"]
perplexity = ["orxhestra[perplexity]>=0.0.58"]
openrouter = ["orxhestra[openrouter]>=0.0.58"]
upstage = ["orxhestra[upstage]>=0.0.58"]
all = ["orxhestra[all]>=0.0.58"]

[project.scripts]
orx-coder = "orxhestra_code.main:main"

[project.urls]
Homepage = "https://github.com/NicolaiLassen/orxhestra-code"
Repository = "https://github.com/NicolaiLassen/orxhestra-code"

[build-system]
requires = ["setuptools>=70"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]
include = ["orxhestra_code*"]

[tool.uv]
dev-dependencies = [
    "pytest>=9.0",
    "pytest-asyncio>=0.25",
    "ruff>=0.11",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.ruff]
target-version = "py310"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]
